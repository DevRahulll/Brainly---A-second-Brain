services:
    mongodb:
        image: mongo
        container_name: brainly
        ports:
            - "27017:27017"

        environment:
            MONGO_INITDB_ROOT_USERNAME: admin
            MONGO_INITDB_ROOT_PASSWORD: password

        volumes:
            - brainly_data:/data/db

volumes:
    brainly_data:
